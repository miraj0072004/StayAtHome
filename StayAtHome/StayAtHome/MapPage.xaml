<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             mc:Ignorable="d"
             x:Class="StayAtHome.MapPage">
    <ContentPage.Content>
        <StackLayout>
            <!--<Label Text="{Binding Longitude}"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <Label Text="{Binding Latitude}"
                   VerticalOptions="CenterAndExpand" 
                   HorizontalOptions="CenterAndExpand" />-->

            <maps:Map HorizontalOptions="FillAndExpand"
                      VerticalOptions="FillAndExpand"
                      x:Name="locationMap"
                      Margin="0,0,0,10"
                      IsShowingUser="True"
                      MapType="Street"
                      />
            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                <Button x:Name="StartJourneyButton"
                        Text="Start"
                        HorizontalOptions="Center"
                        Command="{Binding StartJourneyCommand}"></Button>
                <Button x:Name="resetButton" Text="Reset"></Button>
            </StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                <Label x:Name="elapsedDistance" Text="{Binding ElapsedDistance}"></Label>
                
                <Label x:Name ="elapsedTime"></Label>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>